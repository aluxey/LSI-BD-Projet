<div class="space-y-4">
    <!-- Sujet 1 -->
    
    {% for forum in forumsEvent %}
    <div class="bg-[rgba(24,24,27,0.7)] p-3 rounded-lg hover:bg-[rgba(24,24,27,0.9)] cursor-pointer">
        <div class="flex items-start">
            <div class="w-10 h-10 rounded-full bg-blue-500 flex items-center justify-center mr-3">FE</div>
            <div class="flex-1">
                <div class="flex justify-between">
                    <h3 class="font-medium">{{ forum.titre }}</h3>
                </div>
                <div class="flex items-center mt-2 text-xs text-gray-400">
                    <span
                        class="ml-auto px-2 py-0.5 rounded-full bg-[rgba(76,175,80,0.2)] text-green-500">Evenement</span>
                </div>
            </div>
        </div>
    </div>
    {% endfor %}
    
    {% for forum in forumsProjet %}
    <div class="bg-[rgba(24,24,27,0.7)] p-3 rounded-lg hover:bg-[rgba(24,24,27,0.9)] cursor-pointer">
        <div class="flex items-start">
            <div class="w-10 h-10 rounded-full bg-blue-500 flex items-center justify-center mr-3">FP</div>
            <div class="flex-1">
                <div class="flex justify-between">
                    <h3 class="font-medium">{{ forum.titre }}</h3>
                </div>
                <div class="flex items-center mt-2 text-xs text-gray-400">
                    <span
                        class="ml-auto px-2 py-0.5 rounded-full bg-[rgba(76,175,80,0.2)] text-green-500">Projet</span>
                </div>
            </div>
        </div>
    </div>
    {% endfor %}
</div>